import { APP_CTRL_DOWN, APP_CTRL_UP } from '../ActionTypes';

export const ctrlDownAction = () => ({
  type: APP_CTRL_DOWN,
});

export const ctrlUpAction = () => ({
  type: APP_CTRL_UP,
});
